# REL-v0.0.5 - Release v0.0.5

## Сводка релиза

- **Дата:** 2026-02-18
- **Предыдущая версия:** v0.0.4
- **Новая версия:** v0.0.5
- **Тип релиза:** patch
- **Тег:** v0.0.5

## Чеклист предрелизных работ

- [x] Активная ветка: `main` или `master`
- [ ] Все изменения закоммичены (`git status` чистый)
- [x] Версии в манифестах синхронизированы с тегом релиза
- [ ] Форматирование проходит успешно
- [ ] Линт проходит успешно
- [x] Компиляция/типизация проходит успешно
- [x] Сборка проходит успешно
- [x] Dry-run публикации прошёл успешно (если применимо)
- [x] Публикационное окружение и credentials проверены

## Черновик Changelog для текущей версии

### Features

- Нет продуктовых feature-изменений в диапазоне релиза.

### Fixes

- Обновлены поля `repository` и `bugs` в `package.json` для корректной мета-информации пакета.

### Refactor / Chore

- Нет изменений категории refactor/chore.

### Docs / Tests / Build

- Обновлён release workflow prompt и записи changelog для релизного процесса.

### Breaking Changes (если есть)

- Нет.

## План действий (стеко-ориентированный)

### TypeScript / Node / Bun

1. Зафиксировать изменения версии `0.0.5` и REL-документа отдельным release-коммитом.
2. Запустить quality gate: `npm run typecheck`, `npm run build`.
3. Выполнить проверку публикации: `npm publish --dry-run`.
4. Создать тег релиза и отправить в origin.
5. Выполнить реальную публикацию в npm registry.

### Python

- N/A для данного репозитория (публикуемый пакет Node/TypeScript).

### Другое / Custom

1. Перед публикацией выполнить `npm pkg fix` для автоисправления замечаний npm к `package.json`.
2. Проверить, что рабочее дерево чистое после release-коммита.

## Блокеры и риски

- [x] Блокер: рабочее дерево не чистое (`package.json` обновлён до `0.0.5`, добавлен REL-документ) — до публикации требуется коммит `chore(release): prepare v0.0.5`.
- [ ] Риск: npm сообщил предупреждение об автокоррекции поля `repository`; рекомендуется выполнить `npm pkg fix` и проверить diff перед финальным коммитом.

## Команды для публикации (не запускать до устранения блокеров)

```bash
npm pkg fix
npm run typecheck
npm run build
npm publish --dry-run

git add -A
git commit -m "chore(release): prepare v0.0.5"
git tag v0.0.5
git push origin main --follow-tags

npm publish
```
